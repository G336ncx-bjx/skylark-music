# 更新日志

本项目的所有重要变更都会记录在这里，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [3.3.23] - 2026-09-20

### 修复（电脑版：桌面歌词「用着用着自己消失」）

- 原因：桌面歌词是「无边框 + 置顶 + 不在任务栏显示 + 工具窗口」，系统在几种情况下会把它弄丢，
  而任务栏里没有它的按钮，用户自己还原不了：
  - 按 **Win+D**（显示桌面）会把它最小化，之后再也弹不回来；
  - 某些全屏程序、桌面整理/录屏软件会把它的**置顶样式顶掉**，于是被别的窗口盖住；
  - 拔掉外接显示器、改分辨率或缩放之后，它可能**整个留在屏幕外面**。
- 现在桌面歌词每 1.5 秒做一次**体检并自愈**：被最小化（等 1.5 秒，避免挡住「看桌面」）→ 自己恢复显示；
  置顶被抢 → 重新置顶（不抢焦点）；跑到屏幕外 → 拉回主屏底部居中。
- 可以在 `--lockcheck` 里复现验证：模拟 Win+D 最小化、用 `SetWindowPos` 抢走置顶，
  都会在几秒内自动恢复（报告里会打印「自愈次数」）。

### 调整（源代码拆分，功能一字未改）

两个平台的主窗口文件都已经涨到三千多行，改起来容易出事，这一版把它们拆开了：

- **电脑版**：`MainWindow.cs` 3425 行拆成 5 个文件（用 C# 的 `partial class`，编译后还是同一个类）：
  `MainWindow.cs`（字段、播放控制、托盘与窗口）1288 行、`MainWindow.Library.cs` 971 行、
  `MainWindow.Playback.cs` 584 行、`MainWindow.Ui.cs` 494 行、`MainWindow.Upload.cs` 180 行。
- **安卓版**：`MainActivity.java` 3211 行拆成 5 个类（同一条继承链，运行时还是一个 Activity）：
  `AppShell`（基础：调色板、控件工厂、通用刷新）844 行 → `LibraryScreen`（音乐库、歌单、多选、上传）781 行
  → `PlayerScreen`（播放条、队列、歌词）759 行 → `SettingsScreen`（设置、更新）820 行 → `MainActivity`（生命周期）176 行；
  另外把列表适配器单独拆成 `SongAdapter.java` 197 行。

拆分是**纯搬移**，逻辑一行没动，并且两边都做了校验：

- 电脑版：拆分前后各渲染 12 张界面截图（深浅两套主题、音乐库 / 歌词 / 队列 / 设置 / 桌面歌词 / 批量编辑 / 下载弹窗），
  **逐字节完全一致**；无界面自检通过。
- 安卓版：脚本比对了拆分前后的 122 个方法，方法体逐字符一致（唯一差异是适配器改成独立类后，
  两处构造调用需要多传一个 `this`），字段区也完全一致；构建时的 33 项逻辑自检通过。

对使用者来说这一版没有任何界面或行为变化，装不装都行；它只是让后面加功能时不容易踩到雷。

## [3.3.22] - 2026-09-19

### 修复（数量口径：重复的歌被算了两次）

- 两端统一按「**歌名 + 歌手**」去重：一首歌同时放进两个歌单，只算一首。
  电脑版侧栏「音乐库」和「全部歌曲」、安卓版音乐库首页「全部歌曲」后面那个数字，
  现在等于你实际看到的曲目数（例如云盘上 113 个文件、其中 21 个是歌单间的重复拷贝，显示就是 92 首）。
- 安卓版「全部歌曲」列表以前会把重复的拷贝也列出来，现在和电脑版一致：只显示一次，
  想看那份拷贝点进具体歌单即可。

### 修复（空歌单不显示）

- 以前歌单是从「歌曲所在的文件夹」推出来的，所以**刚建好、还没放歌的歌单根本看不见**，
  用户会一头雾水（建完了却找不到）。现在改成：扫描时把**文件夹本身**也记下来，
  空歌单照常出现在列表里，后面跟着「0 首」。
- 电脑版侧栏、安卓版音乐库首页、「加入歌单」和上传目标都走同一份歌单列表，
  所以空歌单可以直接被选中「上传」或「加入歌单」，不用先随便放首歌进去。
- 点进空歌单不再是「没有匹配的歌曲」，而是提示「这个歌单还是空的」。

## [3.3.21] - 2026-09-19

### 修复（批量编辑：全选之后取消不掉）

- 两端都一样：全勾上之后按钮会变成「**取消全选**」，再点一下就全部取消（以前点完全选就没法反悔）。
- 勾选数量变化时按钮文字跟着变，不用退出批量编辑。

### 修复（电脑版点「完成」会崩）

- 原因是退出批量编辑时先把列表切回「单选」，而这时里面还留着十几项选中，
  WPF 会在这种状态下直接抛异常。现在改成**先清空选中再切回单选**，
  点「完成」、切页面、点批量编辑按钮退出，三条路都不会再崩。

## [3.3.20] - 2026-09-19

### 调整（电脑版：多选改成「批量编辑」，不再要求按 Ctrl / Shift）

- 音乐库和播放队列顶部多了「**批量编辑**」按钮：点一下，列表每行左边出现复选框，
  点一行就是勾一首（不用记快捷键），标题右边换成「已选 N 首 / 全选 / 下载 / 加入歌单 / 删除 / 完成」。
- 退出批量编辑（或切到别的页面）就恢复成「点一行即播放」。
- 侧栏的「全部歌单」改叫「**全部歌曲**」，跟里面装的东西对得上。

### 调整（电脑版：弹窗重新设计）

- 下载、加入歌单、新建歌单、重命名歌单、删除确认全部换成**应用内卡片**：
  跟主界面同一套配色、圆角和按钮，不再弹那种跟主题打架的系统窗口；
  点卡片外面的空白处或按 Esc 就能关掉，弹窗开着的时候空格、方向键不会误触播放。
- 下载卡片把「保存到」做成一块信息区，右边直接「更改…」；加入歌单卡片里是一列歌单，
  选好点「加入」，没有合适的歌单可以当场「新建歌单…」。

### 调整（安卓版：音乐库分层，多选不再挤掉列表）

- 音乐库第一层是**歌单（文件夹）列表**：「全部歌曲」+ 每个歌单（显示有多少首）+「＋ 新建歌单」，
  点进去才是歌曲列表，长按歌单可以改名 / 删除；返回键也是先回到歌单这一层。
- 歌单里面顶部两行是「返回 / 歌单名 / 播放 / 上传」和「搜索 / 排序」；
  **进入多选时这两行原地换成**「完成 / 已选 N 首 / 全选」和「下载 / 加入歌单 / 删除」，
  不再额外多占两行——之前多选时一屏只剩四首歌，现在列表高度不变。
- 播放队列的多选同样改成「原地替换」，不再把列表往下挤。
- 「排序」菜单里多了「重新扫描云盘」，进歌单本身不再触发一次全盘扫描（进去更快）。

## [3.3.19] - 2026-09-19

### 新增（歌单：云盘上的一个文件夹就是一个歌单）

- 电脑版侧栏多了「歌单」一栏：点一下只看这个歌单，右键可以**重命名 / 删除**，下面还有「＋ 新建歌单」；
  「全部歌单」里同一首歌出现在多个歌单时只显示一次（进具体歌单能看到那份拷贝）。
- 安卓版音乐库顶部也多了一排歌单：点一下筛选，长按改名 / 删除，最后一个是「＋ 新建」。
- 歌单就是云盘上的文件夹：一首歌要进两个歌单，两个文件夹里各放一份音频 + 歌词（用服务端复制，不重传）；
  改名用服务端搬家，秒完成。
- **上传会落到当前歌单**（没选歌单就进「默认歌单」，都没有才传根目录）。

### 新增（下载到本地：想把歌拷走听的时候用）

- 电脑版每行右侧有下载按钮，音乐库和播放队列都能 **Ctrl / Shift / Ctrl+A 多选**，
  再点顶部的「下载 / 加入歌单 / 删除」对整片选中批量操作。
- 安卓版**长按列表里的歌**进入多选，出现「下载 / 加入歌单 / 移除或删除」工具条。
- 下载时可以选**只下音频 / 只下歌词 / 两者都下**；电脑版会记住上次选的目录，
  安卓版统一放进系统的「下载/云雀」目录（Android 10 及以上走 MediaStore，不用给存储权限）。
- **从云盘删除**（音乐库那侧）是直接从云盘上删，两端都有二次确认；删掉只能去云盘的历史记录里翻。

### 修复

- 电脑版播放队列的「移除选中」一次能移除多首，正在听的那首被移除时会自动接着播下一首，
  整队列清空时才停止播放。
- 电脑版音乐库列头补上「下载」那一列，列头和下面的内容重新对齐。

## [3.3.18] - 2026-09-18

### 调整（两端音乐库顶部的按钮）

- 去掉了音乐库顶部的「随机播放」按钮：它跟底部那个播放模式重复，含义还容易打架
  （下面选着「列表循环」，上面点一下「随机播放」到底算什么？）。
- 现在**播放顺序只有底部那个模式说了算**：「播放全部」按当前模式播——模式是随机就
  先把整张列表打乱一次再顺序播，其它模式按列表原顺序播。顶部只留 上传 / 播放全部（安卓还有 排序 / 刷新）。

## [3.3.17] - 2026-09-18

### 修复（安卓歌词页「回到当前歌词」按钮）

- **点了没反应**：按钮原来走的是 `applyLyricHighlight`，而它在「当前句序号没变」时会直接返回，
  于是点击后什么都不会发生（按钮照旧留在原地，看起来就是「点不动」）。现在按钮改为**直接平滑滚回当前句**，
  滚到位后按钮自动收起、跟随恢复。
- **换行瞬间闪一下**：自动滚到新句的动画途中，取景会短暂偏离当前句，按钮误判成「你翻远了」而闪出来。
  现在动画期间（约 0.7 秒）不更新按钮显隐，你一动屏幕就立刻接管。
- 自动拽回的行为**保留**：手动滑动后暂停 4 秒跟随，之后唱到新的一句时自动滚回当前句。
  点某一句歌词跳过去听，同样会恢复跟随。

## [3.3.16] - 2026-09-18

### 新增（安卓歌词页：回到当前歌词）

- 安卓歌词页也加了浮动的「**回到当前歌词**」按钮：滚动位置离正在唱的那句太远时自动浮出，
  点一下立刻回到当前句并继续跟随；回到当前句附近自动收起。
- 手动滑动的行为保持不变（滑的时候不会被拽走，松手后暂停 4 秒跟随再自动恢复），
  这个按钮只是提供一个随时可用的手动入口。

## [3.3.15] - 2026-09-18

### 修复（中文歌里的英文/罗马音被当成译文）

- 例：《星辰大海》的 `It's my dream it's magic`、《晴天》里的 `So La Si Si Si Si La Si La So`
  都被当成了上一句的「译文」，于是显示成小字副行（《猜不透》《暖暖》《少年》《有我》也中招）。
- 原因是配对逻辑假设「不是原文语言的行就是译文」，而这些歌**整篇连一个重复时间戳都没有**——
  真正的双语歌词里，原文与译文的时间戳必定成对（要么相同，要么译文被标成下一句的时间）。
- 现在：整篇没有重复时间戳 → 判定为「这首歌没有译文」，一行都不配对，各是各的。
  两端的解析器同步修改，并加了自检用例（中文歌里的英文副歌不算译文）。
- 7 首外文双语歌不受影响，配对与原来一致。

## [3.3.14] - 2026-09-18

### 修复（电脑端歌词页：滑动时被拽回当前句）

- 以前手动滑动只暂停 6 秒，之后**每唱到新的一句都会把视图滚回当前句**，翻歌词根本翻不住。
- 现在改成：**手动滑动＝暂停自动跟随**，之后不管唱到哪一句都不再动你的位置；
  底部会浮出「回到当前歌词」按钮，点它恢复跟随；直接点某一句歌词（跳过去听）也会恢复；
  换一首歌会自动恢复跟随。

## [3.3.13] - 2026-09-18

### 修改（「随机播放」现在是真的打乱列表）

- 以前是「列表顺序不动，每切一首再随机抽一首」：列表里看到的顺序不是播放顺序、当前播放的高亮到处乱跳，
  而且**预取永远对不上**——随机抽去下载的往往不是接下来要播的那首，还会把真正要播的那首挤出
  「只留两首」的名额，于是随机切歌常常要现下、卡一下。
- 现在改成：**点「随机播放」时把播放列表真的打乱一次**（当前正在听的这首排在第一位，不打断），
  之后就像列表循环一样**按这个顺序一首首往下放**。抽签只发生在点随机的那一刻，不会每首再抽一次。
- 好处：列表显示的顺序＝真实播放顺序；随机模式的「预取下一首」终于有效；按「上一首」也是回到列表里的上一首。
- 关掉随机播放会**恢复你原来的列表顺序**（原顺序记在设置里，重启也不丢）。
  两端同步：电脑端存进 `settings.json`，安卓端存进首选项。

## [3.3.12] - 2026-09-17

### 调整（安卓应用内更新的凭证与校验）

- 更新仓库改用**只读** API 令牌（App 只需要列目录和下载），读写令牌只在发版时使用、
  不进 APK。这样即使只读令牌随 APK 公开，也没人能往仓库里改东西。
- 新增**安装前校验**：下载完先检查这个包的**版本号**是否与仓库里标的一致、
  **签名**是否与当前装着的 App 完全相同；任何一条不符就直接删掉并提示，
  不去惊动系统安装器。这样就算更新仓库被人塞了改过的包，也装不上。

## [3.3.11] - 2026-09-17

### 调整（安卓应用内更新的下载源）

- 更新源从 GitHub Releases 改成**云盘上的专用仓库**（你新建的那个 `apk` 资料库）：
  国内的网络环境下稳定得多。更新包仍然约定命名 `Skylark-android-<版本>.apk`，
  放在那个仓库里，App 拉取时和歌曲库互不影响。
- 行为不变：只有点「检查更新」才联网；在应用内下载（带进度）；装完自动删掉安装包。
  令牌只对这个 `apk` 仓库有权限，即使被人拿去塞别的包，Android 也会因为签名不同拒绝安装。

## [3.3.10] - 2026-09-17

### 调整（安卓设置页）

- 「检查更新」不再单独占一张卡片，直接并进设置页最下面那张**「关于」**卡片里：
  版本号、项目主页、检查更新放在一起，不再和底部的关于分成两块。

## [3.3.9] - 2026-09-17

### 调整（安卓应用内更新）

- 更新检查改成**只有点「检查更新」才会执行**：不在启动时自动查、不在扫描曲库时顺手查、
  也不会自己弹窗。设置页的说明里写清楚了这一点。
- 更新源默认改为 **GitHub Releases**（读 `releases/latest` 的 `tag_name` 与
  `Skylark-android.apk` 资产），不再要求把安装包放进云盘；GitHub 连不上时，
  才退回云盘里那份同名安装包（如果你自己放了 `Skylark-android-<版本>.apk`）。

## [3.3.8] - 2026-09-17

### 新增（安卓：应用内更新，不用再去网页下 APK）

- 设置页多了「关于与更新 → 检查更新」。**只有点它才会去查**，平时不联网检查、也不会自己弹窗。
  发现新版本后点一下即可：显示当前版本 → 确认 → 应用内下载（带进度）→ 拉起系统安装界面。
  默认从 GitHub Releases 取安装包；GitHub 连不上时会退回云盘里那份同名安装包（如果你放了）。
- **装完自动删安装包**：安装包下载在应用自己的缓存目录里，启动时如果发现它的版本
  已经不高于当前安装的版本（说明刚更新完，或者用户放弃了这次更新），直接删掉。
- 没有引入任何第三方库：为了把 APK 交给系统安装器（Android 7 起不允许 `file://`），
  自带了一个几十行的最小 `ContentProvider`，不依赖 AndroidX 的 FileProvider。
- Android 8 起系统要求用户允许「安装未知应用」，没有权限时会提示并直接跳到那个设置页。

## [3.3.7] - 2026-09-17

### 修复（云端歌词改了，客户端却还用旧缓存）

- 安卓的歌词缓存文件名以前只跟歌曲路径有关，**云端歌词更新后它仍然读旧缓存**
  （这也是之前「重启客户端怎么还是老样子」的原因之一）。现在缓存名里带上云盘上
  歌词文件的修改时间（`l5-<路径+修改时间>`），云端歌词一改就自动重新下载，
  不用再靠升版本号作废缓存。
- **歌词缓存以前从不自动删**：音频一直是「关着缓存开关时只留正在听的和下一首」，
  但歌词那边一直留着不删（代码里甚至专门跳过 `lyrics/` 目录）。现在歌词缓存走
  同一套策略：默认只留正在听的那一首和下一首的歌词，更早的自动删掉；打开
  「听过的歌都留在本机」时都留着，但每首歌只留最新一版。换过前缀的老缓存
  （`l1-`…`l4-`）会一次性清掉。歌词一个文件只有几 KB，主要是别再留着没用的东西。

### 修复（《Take Me Hand》整首晚 1.07 秒）

- 和网易云官方时间轴逐行比对：65 行**整齐地全部偏移 1.07 秒**（官方
  `[00:59.060] In my dreams` ↔ 我们 `[01:00.13]`），所以是整条时间轴偏晚。
  云端歌词已整体提前 1.07 秒，现在与官方逐行一致。

## [3.3.6] - 2026-09-17

### 修复（安卓端歌词「稍微延后」）

- 安卓的进度条每 500ms 刷一次，**歌词也跟着这个节拍更新**，于是最多要等半秒才跳到
  下一句，听起来就是「唱到了才慢慢换句」。现在歌词用独立的 120ms 节拍刷新，
  进度条保持原来的频率（省电）。电脑端本来就是插值刷新，不受影响。

### 修复（《クリームソーダとシャンデリア》整首早一句）

- 用云盘上那首音频实测：**开头 0–4.4 秒几乎无声音**（`mean_volume: -53 dB`，
  4.4 秒起才有音乐），而歌词第一句标在 0.33 秒 —— 整条时间轴比实际音频早了一整句，
  所以会出现「上一句还没唱完就跳到下一句」。现在该歌词整体后移 4.1 秒，
  第一句落在 4.43 秒，和音频起点对齐。
- 其余 6 首外文歌的音频开头静音都只有 0.4–0.8 秒，且首句歌词远在其后，不受影响。

### 新增（开发用）

- 命令行 `--lyricshift <文件或目录> <秒数>`：把歌词整体平移若干秒（负数即提前），
  用于修正整条时间轴偏移的歌词；顺带把 `--lyricfix` 的规范化逻辑抽成可带平移量的版本。
- 安卓歌词缓存前缀升到 `l4-`，装新版后会自动重新取歌词。

## [3.3.5] - 2026-09-17

### 修复（桌面歌词的解锁按钮「怎么缩都还是这么大」）

- 找出真正的病根：**按钮的窗口被 WPF 撑大了**。内容是 22 × 20 DIP，窗口却变成
  134.7 × 37.3 DIP（约 6 倍面积），深色背景铺满整块窗口，所以不管把里面的字和图标
  改多小，屏幕上永远是一大块方块 —— 这就是「重启好几次还是这么大」的原因。
  成因是这组属性凑在一起时 `SizeToContent` 会算出错误尺寸
  （无边框 + 窗口透明 + 显示前没设 Left/Top，实测可复现）。
  现在窗口尺寸写死成内容大小（22 × 20 DIP），不再依赖 `SizeToContent`。
- 顺手把按钮做成**纯图标**（12 px，去掉「解锁」两个字），鼠标移上去弹出说明。
- 加了兜底自动隐藏：万一个别环境读不到鼠标位置（`GetCursorPos` 会直接失败），
  按钮也不会永远挂在桌面上，最多 4.5 秒自己收起来。

### 修复（双语歌词在安卓端整首错位）

- **「歌名 - 歌手」这种自动生成的首行**被当成了原文，把真正的第一句歌词当成译文吃掉，
  之后整首歌**错开一行**（表现为「上一句的译文配下一句的原文」）。
  现在这种行会被识别出来并跳过（同时兼容「歌名 (中文译名)」写法）。
- **安卓端全角空格行**（`[00:01.11]　`）没被当成空行：`String.trim()` 不认 U+3000，
  这些行会占掉「原文」的位置，同样导致整首错位。现在按 Unicode 空白处理。
- **原文语言判断更稳**：含有假名的行只可能来自日文原文（中文译文里不会有假名），
  这条优先于 `[ti:]` —— 实测《summertime》标题是英文、歌词却是日文，只看标题会判错。
- **标准排版按时间戳分组配对**：原文与译文时间戳相同时，组内第一行就是原文。
  这样「磊々落々反戦国家」「絶対徹夜」这类纯汉字日文原句也不会被误判成译文。
- 安卓端歌词缓存前缀升到 `l3-`，装上新版后会自动重新取歌词，不再用旧解析结果。

### 其它

- 云端 **7 首外文歌**歌词统一改写为「原文在上、译文在下、两者时间戳相同」的标准排版，
  并逐首核对过：千本桜、可愛くなりたい、願い〜あの頃のキミへ〜、
  インドア系ならトラックメイカー、クリームソーダとシャンデリア、summertime、Take Me Hand。
  （顺便说明：《インドア系ならトラックメイカー》里「等待光标的指示」「看我点击歌词」
  是歌词原本的译文 —— 对应「カーソル待ってよ」「ほらクリック」，不是乱码。）

## [3.3.4] - 2026-09-17

### 修复（安卓端歌词页）

- **文字改为居中**：歌词与译文都整行居中显示，和电脑版歌词页保持一致
  （当前句的垂直居中本来就有：滚动时会自动把正在唱的那句摆到屏幕中间）。
- **滚动时不再冒出滚动条**：歌词区关掉了纵向 / 横向滚动条，滑动时画面干净。
- 顺带一个手感改进：既然整行都居中了，点击区域也变成**整行可点**，
  点哪句跳哪句比原来只有文字能点更好按。

## [3.3.3] - 2026-09-17

### 修复

- **上传同名文件会变成副本**：Seafile 的 `upload-api` 并不认 `replace=1`（实测确认），遇到同名文件会
  **默默改名成「xxx (1).ext」**，于是重传歌词/歌曲时会堆出一串副本。
  现在令牌模式下会**先查、再删同名旧文件、稍等片刻再上传**，确定性地完成替换（实测：同一文件重传后
  仍是同一个文件名，字节数正常更新）。
- **双语歌词的元信息行会打乱配对**：「作词 : 某某」「Lyrics by」这类行以前会被当成译文参与配对，
  导致整首歌词错开一行（《千本桜》《summertime》都中招）。现在识别时容忍冒号前后的空格，
  并且这类行一律单独成行、不参与配对。
- **判断原文语言时会被首行的信息行带偏**：以前用「第一行」判断语言，而第一行常常是「作曲 : 某某」
  （中文），于是把中文当成原文、整首错位。现在改成**用第一句真正的歌词**判断。
- 安卓端歌词缓存加了版本前缀（`l2-`）：之前缓存的旧格式歌词会自动失效并重新取。

### 新增

- 命令行新增 `--lyricfix <文件或目录>`：把双语歌词规范化成「译文与原文同一时间戳」的标准写法
  （两种常见写法都能归一），方便一次性整理整个歌词库。

### 其它

- 用「程序规范化 + 人工逐首复核」的方式，把云端 **7 首外文歌**的歌词全部重做了一遍：
  千本桜、可愛くなりたい、願い〜あの頃のキミへ〜、インドア系ならトラックメイカー、
  クリームソーダとシャンデリア、summertime、Take Me Hand —— 原文与译文现在都严格一一对应，
  时间轴沿用你原来那套（和音频严丝合缝）。

## [3.3.2] - 2026-09-17

### 修复

- **把还写着「不支持 FLAC」的说明全部改掉**（v3.3.0 起 FLAC 已经支持了）：
  - 扫描完成后的提示原本写「已忽略 N 个不支持的格式，如 **FLAC / OGG**」，现在改成 `ape / dsf / amr`；
  - Release 页面说明原本写「v3.0.0 起不再支持 FLAC」，现在列出两端完整支持清单
    （已发布的 v3.3.0 / v3.3.1 说明也一并更新）；
  - 几处代码注释里把 FLAC 当「不支持格式」的例子也改掉了；
  - CHANGELOG 的 3.3.0 条目补了一句说明：FLAC 是 v3.0.0 移除、v3.3.0 又加回来的（现在系统播放器直接支持）。

## [3.3.1] - 2026-09-17

### 变更

- **桌面歌词的「解锁」小按钮**：
  - 尺寸从约 71×29 缩到 **53×19**（图标 11px、文字 11px、更紧凑的内边距）；
  - **显示时机改了**：以前只要鼠标落在歌词窗口范围内就会冒出来，现在**只有鼠标靠近那个按钮本身**
    （右上角那一小块，前后左右留 34px 余量）才显示，移开约 0.9 秒淡出——鼠标扫过歌词时不再被打扰；
  - 锁定时的提示同步改成「把鼠标移到右上角可解锁」；`Ctrl+Alt+L` 仍然随时可用。
- 截图模式新增 `unlock` 视角，方便核对这个按钮的尺寸（它是独立窗口，不会出现在歌词截图里）。

## [3.3.0] - 2026-09-17

### 新增

- **两端都支持更多格式**（实测：Windows 11 自带内核能直接放 flac / aiff）：
  - Windows：mp3 / m4a / aac / wav / wma / **flac** / **aiff** 直接播放；
    **ogg / oga / opus / ape / wv** 在设置里指定 `ffmpeg.exe` 后自动转成 mp3 播放（只转一次，之后走缓存）；
  - Android：mp3 / m4a / aac / wav / wma / **flac** / **ogg** / **opus** 由系统播放器直接播放。
  - 自检新增格式白名单用例（flac / aiff / ogg 进库、不支持的格式计入忽略）。
  - 说明：FLAC 在 v3.0.0 曾被移除（当时用内置解码器，太笨重）；现在 Windows 10/11 与安卓系统播放器
    都能直接放 FLAC 了，所以这一版把它**加回来**，不再需要任何额外组件。

### 修复

- **音乐库「歌手」列看着和列头错位**：其实是「有歌词」的「词」徽标贴在歌手名前面，
  让人以为歌手列从徽标开始。现在把徽标挪到最右侧单独一列，并加了「列头跟随列表实际可视宽度」
  的对齐逻辑（列表里有滚动条时会差十几个像素，现在会自动补偿）。

### 文档

- README 精简为「使用者关心的部分」（下载 / 首次使用 / 功能 / 格式 / 快捷键 / 桌面歌词），
  构建方式、目录结构、命令行工具、签名与发布流程搬到 [docs/development.md](docs/development.md)。
- 截图按新界面重新生成；代码里硬编码的个人音乐目录换成系统的「我的音乐」目录。

## [3.2.11] - 2026-09-17

### 变更

- **电脑版不再显示时长**：音乐库列表与播放队列的「时长」列都去掉了，侧栏的
  「云盘 · 10 首 · 已缓存 6 首 · 42 分」也简化为「云盘 · 10 首 · 已缓存 6 首」。
  有些云盘歌曲解析不出时长、显示成 `--:--` 反而添乱；顺带取消了每首歌 512KB 的时长探测，
  扫描云盘更快，也少占流量（歌词仍然会预取）。

### 修复

- **安卓通知栏卡在「正在缓冲… 90%」**：通知里不再写百分比了（只有还没开始出声时才显示「正在缓冲…」），
  同时服务每 2 秒对齐一次通知栏 / 锁屏状态，即使某次刷新漏了也会在 2 秒内自愈。
  通知里的播放 / 暂停、上一首 / 下一首按钮不受影响，仍与播放状态同步。

## [3.2.10] - 2026-09-17

### 修复

- **日文歌的双语配对还是错的**（《願い～あの頃のキミへ～》）：3.2.9 用「出现次数最多的语言」当原文，
  而这首歌的中文译文里多出「词：/曲：」这类信息行，中文 76 行 > 日文 75 行，
  于是被判成「中文是原文」，整篇配对又反了。
  现在**优先用 `[ti:标题]` 的语言**判断原文（标题一般就是原文语言），其次用第一行（要求占比 ≥ 1/4），
  最后才用多数派。用你云盘上的两个真实歌词文件核对过：
  - 《Take Me Hand》→ 每句英文都配到自己的中文译文 ✓
  - 《願い～あの頃のキミへ～》→ 每句日文都配到自己的中文译文 ✓
- 顺便解释一下之前看到的「中文和英文挨得比英文和它的译文更近」：那不是字体问题，
  而是配对错位后一组「原文+译文」被拆成了两组（组内 3dp、组间 18dp），配对修正后间距自然正常。

### 工具

- 新增 `Skylark.exe --lyricdump <歌词文件>`：把 App 内解析器的结果直接打印成 `[时间] 原文 || 译文`，
  方便核对双语歌词的配对。

## [3.2.9] - 2026-09-17

### 修复

- **外语歌的「上一句译文 + 下一句原文」错位**（桌面歌词与歌词页都有）：
  有些双语歌词把译文写在原文后面，但**时间戳标成了下一句的时间**，例如
  ```
  [00:59.99]In my dreams
  [01:01.66]我的梦里          ← 本意是上一句的译文
  [01:01.66]I feel your light
  [01:03.60]有你的光芒        ← 本意是上一句的译文
  ```
  老解析器只按「同一时间戳取第二行当译文」配对，于是 01:01.66 那一对被读成
  「原文 = 我的梦里、译文 = I feel your light」，就是你看到的错位。
  现在改成**按文件顺序配对**（译文永远紧跟在它自己的原文之后）并沿用原文的时间戳；
  用哪种语言当原文，由整篇歌词的主要语言决定（含假名算日文、只有汉字算中文、其余算拉丁/其它），
  几种语言数量相同时以第一行为原文。Windows 与 Android 两端都已修正。

- 自检（Windows `--selftest`、Android `android\tools\SelfTest.java`）都新增了这种写法的用例，
  现在 Windows 通过 40 项、Android 通过 32 项。

## [3.2.8] - 2026-09-17

### 变更

- **桌面歌词的两行固定成「只差字号」**：第二行（下一句 / 译文）字号为当前句的 **74%**
  （34px → 25px，差得足够一眼分清，又不至于看不清），字重、颜色、亮度与当前句**完全一致**——
  不压暗、不加标记条、不做切句动画，也不再有「下一句」选项。
- 撤掉 3.2.7 引进的「下一句：不显示 / 小一号 / 同样大小」三档选择（用户反馈：不要再做其他选项）。

## [3.2.7] - 2026-09-17

### 变更

- **桌面歌词重新设计「两行同时看清 + 分得清哪句是当前句」**，不再靠压暗第二行
  （压暗在深色桌面上会显得「更黑」），也不用任何额外的标记条：
  - **静态两级排版**：当前句 Bold + 100% 字号，下一句 SemiBold + 84% 字号，**两行都是满亮度**；
  - **动态切句提示**：换到新的一句时，那一行做 240ms 的淡入 + 轻微放大（0.94 → 1.0），
    余光就能看出刚跳到哪一行；
  - 「下一句」仍可选 不显示 / 小一号（默认）/ 同样大小；选「同样大小」时靠字重与切句动画区分。

## [3.2.6] - 2026-09-16

### 修复

- **桌面歌词第二行「整体更黑」**：以前第二行会被压暗（译文 0.92、下一句 0.6~0.8 透明度），
  在深色桌面上看着就是「比上面那句更黑」而不是「更淡」。
  现在**同样大小模式下第二行完全不压暗**，两句亮度一致，只靠字重（当前句 Bold、下一句 SemiBold）与位置区分；
  「小一号」模式也只降到 0.9。
- **文字描边可调**：设置 → 桌面歌词 → 「文字描边」＝ 关 / 弱（默认）/ 强。
  之前是一团模糊光晕，嫌黑可以直接关掉（锁定时没有底色，建议留「弱」）。

### 其它

- 配置校验补上 `lyricOpacity`：文件里缺这个字段时以前会退化成 0.2（歌词整体发灰看不清），
  现在会自动回到 1.0。

## [3.2.5] - 2026-09-16

### 修复

- **桌面歌词第二行「好细看不清」不是字号的问题**：第二行一直用的是 Normal 字重，
  而当前句是 SemiBold，大字号下中文字形的笔画差别非常明显（即使调成「同样大小」也还是细）。
  现在第二行和当前句**同字重**，只用大小与透明度区分。
- **桌面歌词的阴影改成描边**：以前是 `BlurRadius = 9` 的一团模糊光晕，
  浅色字配上它会把笔画边缘「吃掉」，看着又细又虚；现在收紧到 `BlurRadius = 3`、不透明度 0.95，
  效果更接近描边，笔画是实心的。

## [3.2.4] - 2026-09-16

### 修复

- **退出再打开，播放列表被清空**：`SaveSettings()` 每次都会把「内存里的当前列表」写回配置，
  而启动早期云盘还没扫完、内存列表还是空的，这一次自动保存就把配置文件里保存的播放列表覆盖成了空，
  于是「恢复上次列表」那一步也没东西可恢复了（表现就是列表没了、进度也不接着放）。
  现在只有在「已经恢复过上次的列表」或「列表里确实有歌」时才写回队列。
  - 实测（隔离配置，队列 3 首、听到第 2 首 42 秒）：修复前退出后队列变成 0 首，修复后 3 首与 42 秒都保留。

### 其它

- **自检 / 截图 / 冒烟等命令不再读写 `%APPDATA%\Skylark`**：这些模式现在用临时数据目录，
  不会再把你的播放列表和设置覆盖掉（之前跑一次截图就可能把队列写成一堆演示数据）。

## [3.2.3] - 2026-09-16

### 变更

- **桌面歌词的「下一句」默认和当前句同字号**，只靠字重（当前句加粗）和透明度区分，
  不再小到看不清；设置 → 桌面歌词 里新增「下一句」选项：**不显示 / 小一号 / 同样大小**。

### 修复

- **解锁时第二行字号会瞬间变小**：解锁会重新走一遍 `ApplySettings()`，那里还留着旧的
  「第二行 = 主行 60%」的赋值，所以每次解锁都会闪一下小字。现在三处（应用设置、显示译文、显示下一句）
  都用同一个字号计算。

### 其它

- `build.ps1` 增加 `-OutDir` 参数，可以在程序正在运行时把 exe 编到别的目录（不影响正在用的版本）。

## [3.2.2] - 2026-09-16

### 变更

- **Android 也改成「留两首」策略**（关掉缓存时）：本机保存正在听的那一首 + 下一首，
  切歌几乎不用等，占的空间很小；打开缓存则是听过的歌都留在本机。
  - 原因是「直接把 URL 交给播放器在线播放」不可靠：云盘给 mp3 返回的是
    `Content-Type: audio/mp3`（不是标准的 `audio/mpeg`）外加 `X-Content-Type-Options: nosniff`，
    Android 播放器据此匹配不到解复用器、又不会去嗅探文件头，表现就是**一直卡在「正在缓冲」**。
    改成先取到本机再交给播放器，彻底绕开这个问题（电脑版同理，所以它一开始就是下载后播放）。

### 新增

- **歌词页字号可调**：设置 → 桌面歌词 → 「歌词页字号」（12–40 px，默认 16），
  调的时候歌词页会立刻跟着变，不用重新点歌。桌面浮窗的字号还是上面那一项，两者互不影响。

### 修复

- **桌面歌词的「下一句」看不清**：以前只有主行字号的 60%、透明度 60%，看起来跟只显示当前一句差不多；
  现在放大到 78%、透明度提到 85%。

## [3.2.1] - 2026-09-16

### 修复

- **电脑版关掉缓存后不再预取下一首**，导致每次切歌都要等下一首下完。
  现在恢复成「本机保留两首：正在听的那一首 + 下一首」，切歌几乎不用等，
  切到第三首时最早的那一首会被删掉，始终只占两首的空间。
- Android 端行为不变（关掉缓存就是完全不落地）。

## [3.2.0] - 2026-09-16

### 变更

- **默认不再把歌留在本机**：既然曲库在云端，本地就不该越用越满
  - **Android**：改为直接在线播放（HTTP 流，服务端支持 Range 所以拖进度也能用），
    点歌即播、不用等整首下载完，**硬盘上不留任何音频文件**；
  - **Windows**：WPF 播放内核不支持网络流（实测 HTTP 源解码失败），所以本机保留正在听的那一首和下一首，
    切到后面时自动删掉更早的，始终只占两首。
- **设置里一个开关控制缓存**（两端一致）：**开 = 听过的歌都缓存到本机**（可离线播放，也会预取下一首）；
  **关 = 不缓存**，Android 是真·不落地，Windows 受播放内核限制只能临时下载正在听的那一首，切歌 / 退出立刻删掉。

### 修复

- **「正在缓冲…」状态卡住**：播放准备好之后没有清掉缓冲标记，会出现「都播了两分钟还显示正在缓冲 83%」。
  现在准备就绪 / 切歌 / 出错都会正确清掉。
- **音乐库列表一直高亮正在播放的歌**：高亮属于播放队列，音乐库恢复成普通列表。
- **进度条重做**：轨道、已播放部分、圆形滑块三者对齐（此前圆点和色条端点对不上），
  左右留出滑块位置，滑到两端不再被切掉。
- **列表滚动更顺**：歌曲时长、歌词偏移这些按歌曲记录的小数据改为内存缓存，
  不再每画一行就解析一次 JSON。

## [3.1.0] - 2026-09-16

### 新增

- **Android 版（`Skylark-android.apk`）**：功能与 Windows 版对齐，共用同一个云盘曲库，
  下载 APK 直接安装即可使用（Android 5.0 及以上），不需要应用商店、不需要 Gradle。
  - 四个页面：音乐库（搜索 / 播放全部 / 随机播放 / 排序 / 刷新 / 上传）、播放队列、歌词、设置；
  - **点歌名 = 立刻播放并加入队列**（同一首不会重复添加），队列支持移除、上移下移、设为下一首、清空；
  - **播放队列与播放位置会保留到下次打开**，不用每次重新点；
  - 歌词页：逐行高亮 + 自动居中、点句跳转、`±0.5 秒` 偏移（按歌曲记忆）、译文行；
  - 设置页：分享链接 / API 令牌（带「粘贴」按钮）、测试连接、缓存开关与清理、恢复隐藏歌曲、关于；
  - **主题默认跟随系统**，也可以固定浅色 / 深色；
  - 前台播放服务 + 通知栏 / 锁屏 / 耳机按键控制，切到别的 App 或锁屏后继续播放；
  - 支持在别的 App 里「分享」音频文件给云雀，直接上传到云盘；
  - 手机端不做桌面歌词（手机上用不上）。
- Android 构建链路完全不依赖 Gradle：`aapt2` → `javac` → `d8` → `zipalign` → `apksigner`；
  签名密钥与口令只留在本机（`android/skylark.jks` + `android/keystore.pass`，都在 `.gitignore` 里），
  发布时由 GitHub Secrets 提供同一份密钥，保证后续版本能直接覆盖安装。
- 新增纯 Java 逻辑自检 `android/tools/SelfTest.java`（文件名解析、编码识别、MP3 时长、LRC 解析 27 项），
  构建前自动运行；CI 与 Release 现在会同时构建 Windows exe 和 Android APK。

### 说明

- Release 附件新增 `Skylark-android.apk`，`SHA256SUMS.txt` 同时给出 exe 与 apk 的校验值；
- 两个平台都是未签名的开发者自签名程序，首次安装会提示「未知发布者 / 未知来源」，属于正常现象。

## [3.0.0] - 2026-09-16

### 变更（不兼容）

- **移除 FLAC 支持**：删除内置 FLAC 解码器与相关代码，扫描时不再收录 `.flac`（以及 ogg / opus / ape / wv 等）；
  遇到这类文件会在扫描提示里说明忽略了多少个。曲库统一使用 mp3 / m4a / wav / wma / aac，
  播放链路更短更稳，也不再产生临时解码文件。

### 说明

- 同时把两首无损（铁道唱歌·京广 / 京沪高铁篇）换成 320kbps MP3：MP3 与歌词已上传、云端与本地 flac 均已清理。
## [2.0.1] - 2026-09-16

### 修复

- **FLAC 第一次播放没声音**：云端歌曲下载完成后，代码把 FLAC 原文件直接交给了系统播放内核（它不支持 FLAC），跳过了内置解码那一步，表现就是"点了没反应"。现在下载完成会重新按格式决定播放方式，FLAC 照常解码成 WAV 再播。
- **重播同一首歌会重新下载**：在「关闭缓存（播完即删）」模式下，重新点当前正在播的这首会把它自己的缓存删掉、再从云端下一次。现在同一首重播直接复用缓存。
- **同一文件重复打开会卡二十多秒**：系统播放内核重复 `Open` 同一个文件响应极慢，现在同一首重新点播放改为「跳转 + 播放」，立刻响应。
- 解码 WAV 改为先写 `.part`、全部解完再改名，避免中断留下半截文件被当成正常音频；启动清理也只删 3 小时以上的陈旧文件，不再触碰正在播放的。

## [2.0.0] - 2026-09-16

### 变更

- **项目改名为「云雀 / Skylark」**（原「轻音乐 / Light Music」）：程序名、窗口标题、托盘、快捷方式、
  安装脚本、构建产物 `Skylark.exe`、GitHub 仓库地址全部更新为 `G336ncx-bjx/skylark-music`（旧链接会自动跳转）。
- 配置与缓存目录由 `%APPDATA%\LightMusic` 迁到 `%APPDATA%\Skylark`：**首次以新名字启动时自动迁移**，
  令牌、播放列表、设置与已缓存的歌曲都会带过去（搬不动的缓存会重新下载，不影响使用）。
- 应用图标换成「天空蓝」配色，更贴合"云"的感觉。

### 文档

- README 增加「第一次运行提示『未知发布者』怎么办」说明：这是未签名程序的正常提示，
  点「更多信息 → 仍要运行」即可；同时给出 SHA256 校验与自行构建的替代方案。
- Release 页面也开始附带 `SHA256SUMS.txt`，方便核对下载到的 exe。

## [1.6.0] - 2026-09-16

### 新增

- **内置 FLAC 解码器**：无损格式不再需要用户安装任何东西——播放 FLAC 时由播放器自己解码成 WAV（临时文件，切歌即清理）再交给系统播放。正确性用 ffmpeg 做了**逐字节 PCM 比对**：102MB 的解码结果 MD5 完全一致。
- FLAC 不再依赖 ffmpeg；OGG / OPUS 等冷门格式仍在检测到 ffmpeg 时自动转码播放，设置页会显示当前状态。
- 新增命令行工具 **`--uploadall <令牌或分享链接> <本地文件夹>`**：把本地文件夹里的音频与 `.lrc` 一次性补齐到云端（云端没有的上传、同名同大小跳过、同名不同大小覆盖），传完会打印清单。适合「先把本地全部传上去，确认无误后删本地」的用法。
- `--cloudtest` 升级为**整库校验**：逐首拉取文件头解析时长，并统计歌词覆盖率，异常文件会列出来（本次用它确认了 27 首全部可解析）。

### 说明

- 用上面的流程把本地 10 首歌 + 歌词 + 一个 64MB 无损文件（约 156MB）传到云盘，**逐文件 MD5 与云端一致**后才清理了本地副本。

## [1.5.0] - 2026-09-16

### 新增

- **播放列表会延续了**：队列与当前曲目、播放进度在每次变化后即时保存，下次打开自动恢复成上次的列表（之前会被「空队列」覆盖，所以每次都是 0 首）。
- 桌面歌词的「解锁」按钮改为**鼠标靠近才出现**，移开约 1.4 秒后自动隐藏（之前一直挂在屏幕上）。
- 桌面歌词排版按有无译文区分：
  - 当前句**有译文**（外语歌常见）：只显示这一句，原文在上、译文在下，**字号与字重相同**，不再挤一行下一句预览；
  - 当前句**没有译文**：仍然显示下一句作为预览。

### 变更

- 歌词页里译文行字号由 12.5 调到 14，外语歌看着更清楚。

## [1.4.1] - 2026-09-16

### 修复

- **只填 API 令牌、分享链接留空也能正常使用**（已实测：列目录、播放、缓存、歌词全部正常）。
- 侧边栏左下角不再显示令牌明文，令牌模式下显示资料库名称（如「云盘（API 令牌）：music」）。
- 令牌模式下右上角「打开云盘」改为打开资料库网页，不再拼出无效的分享链接地址。
- 分享链接为空时点「测试连接」会提示改用「检测令牌」，不再报无意义的错误。

## [1.4.0] - 2026-09-16

### 新增

- **支持「资料库 API 令牌」**（网页版：资料库 → 设置 → API 令牌，权限选读写）：填了令牌就优先直连资料库，不再依赖分享链接，并且可以
  - **直接从播放器删除云端文件**（音乐库右键 → 「从云盘删除…」，会二次确认，文件进云盘回收站）；
  - 上传时自动覆盖同名文件；
  - 在设置页点「检测令牌」查看资料库名称与文件总数。
- 令牌与分享链接使用同一个「云端音乐」卡片配置，填哪个都能用。

### 说明

- 令牌等同密码，只保存在本机 `%APPDATA%\LightMusic\settings.json` 里，不会上传、也不会写进仓库。

## [1.3.0] - 2026-09-16

### 新增

- **上传到云盘**：音乐库顶部「上传歌曲」、设置页「上传歌曲」，或直接把歌曲 / 歌词文件（也可以整个文件夹）拖进窗口即可上传到云盘分享目录；侧边栏显示上传进度，完成后自动刷新列表。
- **歌词颜色可自定义**：设置页提供 10 个预设颜色（新增黑色 / 深灰 / 深蓝 / 深红，浅色背景也能看清）以及「自定义…」取色器；桌面歌词右键菜单也能直接换色。
- **锁定时的解锁按钮**：锁定桌面歌词后，右上角会常驻一个「解锁」小按钮（独立窗口，锁定时依然可点），用鼠标就能解锁；其余按钮只在未锁定时出现。

### 变更

- 歌词页改用 ScrollViewer 承载歌词并做平滑滚动，修复长歌词只能显示前面若干行、滚到后面一片空白的问题；手动滚动 6 秒后自动回到当前歌词。

### 修复

- 修复桌面歌词拖动时与悬停检测冲突可能导致的卡住。
- 修复音乐库改为单选后右键点击报错的问题。
- 修复歌词页上下淡出遮罩在切换主题后颜色不更新的问题。
- 修复设置页「缓存占用」文字与「清理缓存」按钮没有垂直居中的问题。

## [1.2.0] - 2026-09-16

### 新增

- **云端音乐库**：支持把清华云盘（Seafile）分享链接当作音乐库，读取目录、时长与歌词，播放时自动下载到本地缓存。
- 自动预取播放列表里的下一首云端歌曲；缓冲时在播放条底部显示进度。
- 云端缓存管理：显示占用、一键清理，可关闭缓存（播完即删）。
- 新增命令行自检：`--cloudtest <分享链接>` 校验列目录 / Range 解析时长 / 下载 / 歌词解析，`--streamtest` 校验流式播放。

### 变更

- **去掉本地文件夹作为音乐来源**，播放器改为纯云端模式（桌面歌词、播放列表等功能不变）。
- **点击歌名 = 直接播放这一首 + 把它加入播放列表**，不再需要单独的「＋」按钮；重复点击同一首不会重复加入。
- 「下一首播放」如果歌曲已在列表里，会把它移动到下一首的位置，而不是重复插入。

### 修复

- 修复关闭窗口时可能卡住的问题（不再在关闭流程里调用 UnregisterHotKey）。

## [1.1.0] - 2026-09-16

### 新增

- 主题新增「跟随系统」并作为默认选项，系统在浅色 / 深色之间切换时播放器会自动跟随。
- 音乐库顶部新增「播放全部」与「随机播放」按钮。
- 音乐库每行右侧新增 **＋** 按钮：把这一首单独加入播放队列；单击歌名 / 整行即直接播放。
- 播放队列每行右侧新增 **✕** 按钮：逐首移出队列。
- 桌面歌词右上角新增悬停工具栏：锁定 / 解锁、字号加减、回到主界面、关闭，锁定状态下也能用鼠标操作。

### 变更

- 重新设计歌词页：左侧唱片信息卡片（含歌词偏移与桌面歌词开关），右侧逐行歌词按与当前句的距离渐隐、上下淡出遮罩、可直接点句跳转，手动滚动后出现「回到当前歌词」。
- 重新设计设置页为双列卡片布局，音乐库与关于整行展示，右侧不再留白。
- 桌面歌词宽度固定不再跳变；锁定时完全没有背景与阴影，未锁定时鼠标移上去才显示底色与阴影。
- 去掉音乐库序号悬停变成播放键的行为，序号列只用于显示序号与当前播放高亮。

### 修复

- 修复进度条与设置页的滑块：圆形滑点与已播放（已填充）色条的端点现在严丝合缝对齐。

## [1.0.0] - 2026-09-16

第一个可用版本。

### 新增

- 音乐库：扫描本地音乐文件夹，按「歌名 - 歌手」解析歌名与歌手（支持多歌手），支持搜索与按序号 / 歌曲 / 歌手 / 时长排序。
- 播放控制：播放 / 暂停、上一首 / 下一首、进度拖动、音量与静音。
- 播放模式：顺序播放、列表循环、单曲循环、随机播放。
- 播放队列：从音乐库双击播放、下一首播放、添加到队列、移除、上移 / 下移、清空，支持 M3U 导入导出。
- 歌词：软件内歌词页（逐行高亮、自动滚动、点击跳转、翻译行、偏移微调）与桌面歌词浮窗。
- 桌面歌词：无边框置顶、可拖动、可调字号 / 颜色 / 不透明度；**支持锁定（鼠标穿透）**，锁定时不抢焦点、不占 Alt+Tab。
- 音乐目录：默认自动探测 `music` 文件夹，可在设置中更改，也支持把文件夹拖进窗口。
- 外观与体验：深色 / 浅色主题、矢量图标、托盘图标、全局多媒体键、记忆播放进度与窗口位置、单实例运行。
- 工程：`build.ps1` 一键构建（系统自带 csc.exe，产物为单文件 exe）、`scripts\install.ps1` 创建桌面快捷方式、无界面自检 `--selftest`、离屏截图 `--shot`、冒烟测试 `--smoke`、鼠标穿透校验 `--lockcheck`。
